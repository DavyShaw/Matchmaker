import os.path as path


USERS_FILE = path.abspath(
    path.realpath(__file__) + '/../../data/' + 'users.json'
)


EVENTS_FILE = path.abspath(
    path.realpath(__file__) + '/../../data/' + 'events.json'
)
